import 'package:flutter/material.dart';

class EmptyForum extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("No forums available"),
    );
  }
}
